name: IZAKHONO Build to Alpha

on:
  workflow_dispatch:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  alpha:
    # Immutable reviewed policy pin. Upgrade this SHA only through a reviewed Builder change.
    uses: bevanshelton-netizen/izakhono-builder/.github/workflows/izakhono-build-to-alpha.yml@6d791da96ee2c20a0701258d3d4674e0c50e7291
    with:
      manifest_path: .izakhono.json
