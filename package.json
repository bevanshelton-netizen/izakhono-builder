{
  "name": "izakhono-builder",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit --target ES2022 --lib ES2022,DOM,DOM.Iterable --module ESNext src/index.ts && tsc --noEmit --target ES2022 --lib ES2022,DOM,DOM.Iterable --module ESNext src/repository.ts src/internal-repository.ts && tsc --noEmit --noCheck --target ES2022 --lib ES2022,DOM,DOM.Iterable --module ESNext src/secure.ts src/sovereign.ts",
    "deploy:first": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply DB --remote",
    "deploy": "wrangler deploy && wrangler d1 migrations apply DB --remote && wrangler deploy",
    "validate": "npm run validate:portfolio-growth && npm run typecheck && wrangler deploy --dry-run",
    "validate:portfolio-growth": "node --check products/izakhono-ads/app.js && node scripts/validate-portfolio-growth.mjs && node scripts/validate-wave1-conversion.mjs"
  },
  "devDependencies": {
    "typescript": "5.9.2",
    "wrangler": "4.127.1"
  }
}
