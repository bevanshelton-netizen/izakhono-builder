{
  "name": "@izakhono/pay",
  "version": "0.1.0",
  "private": true,
  "description": "IZAKHONO PAY payment orchestration portal",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/core.js && node --check src/index.js && node --test tests/*.test.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply DB --remote"
  },
  "devDependencies": {
    "wrangler": "4.127.1"
  }
}
